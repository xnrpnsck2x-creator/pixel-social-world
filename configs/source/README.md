# Source Configs

Studio Mode treats JSON files in `configs/` as the runtime contract. Source spreadsheets or CSV exports should live here and be converted into JSON before a build.

Planned MVP source tables:

- `scene_routes`
- `chat_channels`
- `housing_items`
- `minigames`
- `economy`
- `fishing`
- `localization`
